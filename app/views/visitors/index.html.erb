<h3>Welcome to Whos Who!</h3>
<p><%= link_to 'Users:', users_path %> <%= User.count %> registered</p>
<%= react_component("GameWindow", props={user: EmployeeSerializer.new(@employee)}) if signed_in? %>
